#ifndef UTILITY_H
#define UTILITY_H

#include <string>

namespace util{
  bool isDigit(const std::string& s);

  bool isRegister(const std::string& s);

  bool isOperator(const std::string& s);
}

#endif
